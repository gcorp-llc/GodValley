apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gcorp-ingress
  namespace: gcorp
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: gcorp.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
