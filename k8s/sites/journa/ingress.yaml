apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: journa-ingress
  namespace: journa
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: journa.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
