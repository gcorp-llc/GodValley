apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zeteb-ingress
  namespace: zeteb
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: zeteb.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
